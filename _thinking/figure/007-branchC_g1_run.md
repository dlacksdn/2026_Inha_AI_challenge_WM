# 007 — branch C 첫 학습(L1 단독)의 근거들

> 만든 날: 2026-08-09 | 원본: `artifacts/branchC/train_20260808_1809_g1/` · `run_logs/`
> 이유: **이 기계(5090)를 다른 사람에게 넘긴다.** 원본 276GB 는 못 옮긴다.
> 여기 있는 것은 "그 결정을 다시 설명할 수 있는 최소한"이다 (이 폴더의 규약).

관련 문서: [010 핸드오프](../residual_plan/010-handoff_to_6000.md) · [009 §10~§13](../residual_plan/009-implementation_and_ruler.md)

---

## 이 런이 무엇이었나

```
2026-08-08 18:09 ~ 23:11   SimVP-TAU 잔차 회귀 · 320×512 · hid_S=64 · N_S=2
손실 = L1 단독 (방향항 안 켬)      A/B 의 기준선 팔
step 3,850 / 6,000 에서 정지 — GPU 를 RTX PRO 6000 으로 교체하기 위해
```

---

## 파일별로 무엇을 증명하나

| 파일 | 무엇을 만든 근거인가 |
|---|---|
| `20260808_1809_train_c_g1_history.json` | **감시 7점 원자료.** 010 §1 표의 모든 숫자가 여기서 나왔다. `wake_step=2000` 이 기록돼 있다 — G1 통과의 증거 |
| `20260808_1809_train_c_g1.log` | **게이트 판정 출력 전문**(8KB). `⭐ wake!` · `→ G2 tie/fail` · λ_c 교정 여부를 사람이 읽는 형태로 남겼다. ⚠ 로그의 `⚠ (a) 16프레임이 하나로 무너지는 중` 은 **오탐**이다(009 §12) |
| `img/20260808_2128_resid_step002500.png` | ⭐ **009 §13 의 핵심 주장**: *"예측 잔차가 회색 블러가 아니라 정답이 밝은 자리에 구조를 갖고 나온다"*. 위=정답 아래=예측, **같은 밝기 자**. 002 §1.2 가 인용한 실패 모드가 안 일어났다는 증거 |
| `img/20260808_2132_cmp_step002500_sample000000.mp4` | 같은 시점 [정답\|예측] 나란히 영상. 잔차비 0.358 이 **화면에 실제로 보이는** 첫 지점 |
| `img/20260808_2132_res_step002500_sample000000.mp4` | 잔차만 뽑은 영상 |

---

## 이 런이 남긴 판정

```
G1  ✅ 통과 (wake = step 2,000. 문턱 2,000 스텝에 아슬아슬하게 걸렸다)
G2  ❌ 미도달 — 발동 시점 step 6,000 인데 3,850 에서 멈췄다
    Δcos 는 2500·3000·3500 에서 +0.030 / +0.025 / +0.035  (문턱 0.046 아래)
```

**멈춘 시점 잔차 코사인 0.374. 필요한 것은 0.96 이다** (009 §1).
방향이 *"대충 맞는"* 단계이지 *"맞는"* 단계가 아니다.

---

## ⚠ 이 자료를 읽을 때

1. **궤적을 외삽하지 마라.** 이 런에서 잔차비는 1000→1500 에 **절반으로 줄었다가** 2000 에 6배로 뛰었다.
   전임 세션은 3점 외삽으로 두 번 연속 틀렸다 (009 §11). 판정은 `gates.py` 가 정한 시점에만.
2. **`film_temporal_cos` 열은 폐기된 구 정의**(DC 미제거)다. 커밋 `888fcf0` 이후 정의가 바뀌었다.
   같은 체크포인트에서 구 0.990 vs 신 0.688 — 새 런 값과 직접 비교하지 마라.
3. 원본 체크포인트 8개(각 117MB)와 나머지 영상은 **5090 기계에만 있다.** 여기 없다.
   재개용 `full_002500.pt` 는 별도로 RTX PRO 6000 으로 옮겼다.
