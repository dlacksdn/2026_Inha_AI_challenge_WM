# === 데이터셋 / 대회 패키지 (외부 배포물, 레포에 올리지 않음) ===
open/
data/
**/data/train/
**/data/eval/
*.parquet

# === 모델 가중치 / 체크포인트 (대용량) ===
*.ckpt
*.pt
*.pth
*.safetensors
*.bin
checkpoints/
backbone.ckpt

# === 생성 산출물 / 로그 (run_logs 등은 로컬 영구보존, 레포엔 미포함) ===
*.mp4
*.avi
input_videos/
run_logs/
outputs/
wandb/
*.log

# === 재현 가능한 산출물(홀드아웃 영상/이미지/생성물). 결과 요약은 results/에 별도 추적 ===
artifacts/

# === [예외] _thinking/figure/ 는 의도적으로 추적한다 ===
# artifacts/ 와 run_logs/ 는 이 컴퓨터에만 있다. 그런데 **방향을 결정한 근거**(그림·판정 출력)가
# 거기에만 있으면 다른 기계에서 작업을 이어받을 수 없다.
# 그래서 "그 결정을 다시 설명할 수 있는 최소한"만 골라 _thinking/figure/ 에 복사해 추적한다.
# 재현 가능한 것(체크포인트·생성 영상 전체·원본 로그)은 여기 넣지 않는다.
!_thinking/figure/
!_thinking/figure/**

# === 제출물 ===
submission_features.csv
submission_kit/input_videos/

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# === OS / 에디터 ===
.DS_Store
Thumbs.db
.idea/
.vscode/

# === OMC 운영 아티팩트 (레포에 미포함) ===
.omc/

# 로컬 캐시(잘못 생성 방지)
.cache/

# === git push 절차 메모 (로컬 전용) ===
# 이 문서에는 개인 PAT 을 붙여넣게 되는 자리가 있다. 한 번이라도 커밋되면 파일에서 지워도
# git 히스토리에 영구히 남고, GitHub 의 secret scanning 이 push 자체를 거부한다(2026-07-31 실제 발생).
# 그래서 저장소에 올리지 않고 각자 로컬에만 둔다.
_thinking/rule/003-git_push.md
