현재 폴더에서 진행중인 프로젝트와 독립적으로 나와 클로드코드가 대화를 나누는 _thinking 이라는 폴더가 있어. (없다면 필요한 첫 순간에 만들어줘) 여기는 001, 002, 003, …마크다운 문서들을 쌓아나가는 공간이야. 내가 명시적으로 “001 문서로 저장” 같은 요청을 할 때만 저장해. 문서의 이름은 적당히 짧게 키워드로 잘 지어줘. 그리고 여기는 append-only 기반으로 되도록 기존 문서를 수정하지 않고 계속 쌓아나갈거야. 그리고 이 폴더는 모든 히스토리가 남기 때문에 명시적인 요청이 있는 상황에만 전체 또는 부분을 읽어.

구현 진행상황을 망각하지 않도록 주기적으로 _thinking을 읽어라.
기존에 쌓아놨던 로그들과 모델들, 데이터를 폐기하지 말고 모아놔라. 언제 다시 쓰일지 모른다
현 모델을 돌리려고 다른 모델의 학습/평가 독립성을 해치지 마라. 현 모델과 다른 모델의 학습/평가 절차는 독립적이어야 한다.

훈련할 때 모델 저장 분기는 넓게 하지 말고 촘촘하게 하라. 중간에 끊어도 날라가지 않게 말이다

훈련을 진행할 때 사용자가 로그를 볼 수 있도록, 로그 파일 위치를 제시하라

모든 문서는 엄밀하게, 초등학생도 이해시킨다는 마인드로 쉽게 작성하라.
문서를 장황하게 길게 늘어뜨리지 말고 핵심만 간결하게 작성하라.

난 한국인이다. 한글로 대답해라.



## Git 커밋·push 정책

- commit은 작업 분기(milestone)마다 자율적으로 한다 (한글 conventional commit).
- commit할 때 push도 같이 자율적으로 한다. 단 아래 가드레일을 엄수한다:
  - push 대상은 해당 프로젝트의 기존 remote·기존 브랜치로만. 새 repo 생성, remote 변경/추가, 새 브랜치 push는 사용자 지시가 있을 때만.
  - force-push(`--force`, `--force-with-lease`) 절대 금지. 히스토리 재작성 금지.
  - push 전 diff에 비밀값(키·토큰)·대용량 파일이 없는지 확인한다.
- pull은 여전히 사용자 지시가 있을 때만 한다 (로컬 작업과의 충돌 방지).

## 기계별 작업 경로·GPU 규칙 (둘 다 공용 컴퓨터다. 이건 매우 중요하다)

**아래 경로 안에서만 수정을 허용한다. 나머지는 전부 읽기 전용이다.**

| 기계 | 작업 허용 경로 | 쓸 수 있는 GPU |
|---|---|---|
| 연구실 5090 (32GB) | `/home/rils/dlacksdn/` | — |
| **RTX PRO 6000 (96GB ×2)** | `/home/video_generation/dlacksdn/` | 🚨 **GPU 0 만** |

- 🚨 **RTX PRO 6000 기계에서는 GPU 0 만 쓴다.** 카드가 2장 보이지만 **GPU 1 은 남의 자리다.**
  모든 학습·추론·측정 명령 앞에 `CUDA_VISIBLE_DEVICES=0` 을 붙인다. 예외 없다.
  `--device cuda` 나 `torch.cuda.set_device` 로 대신하지 마라 — 라이브러리가 몰래 다른 카드를
  잡는 경로가 남는다. 환경변수로 아예 안 보이게 하는 것이 유일하게 확실한 방법이다.
- 데이터(`open/`)는 기계마다 프로젝트 밖에 있고 심링크로만 건다. **읽기 전용으로 취급한다.**
  - 5090: `/home/rils/inha_challenge_datasets/`, `/home/rils/다운로드/train`
  - PRO 6000: `/home/video_generation/open` (→ `open` 심링크 한 줄)
- 파이썬 환경도 기계마다 다르다. 상세는 [_thinking/env_file/001](_thinking/env_file/001-environment_setup.md) §0.
  - 5090: conda `/home/rils/dlacksdn/miniconda3/envs/wm/bin/python`
  - PRO 6000: venv `/home/video_generation/dlacksdn/2026_Inha_AI_challenge_WM/.venv/bin/python`
- 주최 코드가 `../outputs` 같은 상대경로를 쓰면 **공유 폴더에 파일이 생긴다.**
  우리 스크립트는 전부 절대경로로 프로젝트 안에만 쓴다.



## 로그 파일 이름 규칙

`run_logs/` 안의 로그 파일은 **날짜_시간을 이름 맨 앞에** 둔다.

    YYYYMMDD_HHMM_<용도>.log

    예) 20260731_1413_train_r2.log
        20260731_1349_blend_step1000.log
        20260731_1401_vae_roundtrip.log

- **이유**: 이름이 앞에 오면 `ls` 결과가 뒤죽박죽이라, 어떤 로그가 최신인지 알려면 매번
  `ls -lt` 를 쳐야 한다. 날짜를 앞에 두면 `ls` 만으로 시간순이 되고 이름만 봐도 언제 것인지 안다.
- **연도(YYYY)를 넣는다.** 해가 바뀌어도 정렬이 유지된다.
- 같은 분(minute)에 로그가 여러 개 생기는 작업(예: 벤치를 조합별로 연속 실행)은 초까지 쓴다:
  `YYYYMMDD_HHMMSS_<용도>.log`
- `<용도>` 는 무슨 실행인지 짧게 쓴다. 예: `train_r2`, `eval_step1000`, `blend`, `bench_b4_s50`
- **기존 로그 파일 이름은 바꾸지 않는다.** 이미 다른 문서가 파일명을 참조하고 있고,
  기록물은 폐기·변조하지 않는다는 규칙을 따른다. 새로 만드는 것부터 적용한다.
- 긴 작업은 tmux 로 돌리고 로그를 남긴다:

      tmux new-session -d -s <세션명> \
        "<명령> 2>&1 | tee run_logs/$(date +%Y%m%d_%H%M)_<용도>.log"



## 이미지 파일 이름 규칙

생성하는 그림(png 등)도 로그와 같은 규칙을 따른다. **날짜_시간을 이름 맨 앞에** 둔다.

    YYYYMMDD_HHMM_<용도>.png

    예) 20260801_1629_residual_headroom.png
        20260801_1630_branches_curve.png

- **이유**: 로그와 같다. `ls` 만으로 시간순이 되고, 이름만 봐도 언제 그린 것인지 안다.
- **그림은 덮어쓰지 않고 새로 쌓는다.** 데이터가 늘면 같은 스크립트라도 내용이 달라지므로,
  덮어쓰면 "그때는 무엇을 보고 그렇게 판단했는지"를 잃는다.
- 기존 이미지 파일 이름은 바꾸지 않는다. 새로 만드는 것부터 적용한다.
