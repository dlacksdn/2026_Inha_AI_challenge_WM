# 018 — 워핑은 죽었다. 그리고 흐림 예산이 생각보다 훨씬 빠듯하다

> 작성일: 2026-08-01 저녁 | 선행: [016](./016-seed_bug_and_leaderboard_truth.md) §9.2-d(갈림길),
> [017](./017-literature_survey.md)(문헌 조사)
> 성격: 016 §9.2-d 가 남긴 **덧셈이냐 워핑이냐**를 우리 데이터로 판정한 기록.
> 그리고 그 판정을 적대적 검수에 걸어 근거의 구멍 넷을 메운 기록.
>
> 모든 숫자는 홀드아웃 96표본 실측이다. **판정선은 결과를 보기 전에 고정했다.**

---

## 0. 30초 요약

```
[판정]    덧셈 잔차로 간다. 워핑은 버린다.
          완벽한 흐름(RAFT, 정답 영상에서 뽑음)으로 밀어도 뭉갠 덧셈에 진다.
              warp:1   vs addblur4:1   Δ=+0.01247  t=+2.60   ← 킬스위치 발동
              warpc4:1 vs addblur4:1   Δ=+0.01407  t=+2.98

[왜]      "워핑은 원본 픽셀을 옮기니 디테일이 안 뭉개진다"는 전제가 **틀렸다.**
          정확히는 방향은 맞았는데 크기가 무의미했고, 이득이 사는 축에서 압도적으로 졌다.
              DINO   워핑이 −0.00701 낫다   t=−0.88   배점곱 −0.0021   ← 무의미
              Video  워핑이 +0.03128 나쁘다  t=+11.93  배점곱 +0.0094   ← 여기서 진다

[눈으로]  이유가 그림에 그대로 보인다. 우리 데이터의 주된 변화는 **팔이 비켜나며
          가려져 있던 배경이 드러나는 것**이다. 워핑은 프레임 0 에서 픽셀을 당겨오는데
          드러난 자리의 배경은 프레임 0 에 **존재하지 않는다.** 그래서 팔이 안 지워진다.
          덧셈은 팔을 흐릿하게 지우고 배경을 만들어낸다. 없던 것을 만들 수 있다.

[⚠ 새로]  흐림 예산이 **σ ≈ 1.3픽셀**뿐이다. 그 이상 흐리면 잔차 노선의 이득 전체가 사라진다.
          해상도로는 320×512 의 **1/2 이상**을 유지해야 한다.
          ⇒ 문헌이 권한 128×128 설계(017 §7.2)는 **우리 채점에는 너무 작다.**

[⚠ 뒤집힘] 016 의 헤드룸 추정이 **2.2배 낮게** 잡혀 있었다. 016 은 뭉갬 배율을 4배·8배만 쟀다.
          **2배를 재니 −0.03798 이다** (016 이 적은 값은 −0.01690).
          그리고 성분 분해가 뒤집힌다 — k=2 에서는 **DINO 도 이득**이다(0.10271 < static 0.12308).
          ⇒ 016 §9.2 ② 의 "DINO 손해를 감수하고 Video 를 챙긴다"는 **k=4 의 산물이었다. 철회한다.**
          새 그림은 단순하다: **해상도가 곧 이득이다.**
              1/2 −0.03798  ·  1/4 −0.01690  ·  1/8 −0.00380  ·  1/16 −0.00039
          흐림 예산 측정과 **독립적으로 같은 지점(1/2)** 을 가리킨다.

[검수]    "워핑은 방향이 틀려도 덜 다친다"는 **폐기한다.** 바닥 비교가 편향돼 있었다.
          공정하게 다시 재니 **부호가 뒤집혔다** — 워핑이 오히려 +0.00370 더 다친다(t=+2.54).
          ⇒ 워핑은 천장에서도 지고 바닥에서도 진다.

[해소]    "RAFT 가 부정확해서 워핑을 억울하게 죽인 것 아닌가"는 **아니다.**
          프레임별로 재니 픽셀 L1 이득이 t=15 까지 거의 그대로다(−1.70 → −1.46).
          변위가 5배 커지는 동안 흐름은 계속 제 몫을 했다. **진 이유는 표현의 한계다.**

[⚠ 새로]  홀드아웃 96개가 학습 데이터와 겹친다는 것을 **코드로 확정했다.**
          지금은 "학습 데이터에서조차 진다"는 보수적 결론이라 무해하다.
          그러나 C 는 **이기려고 만드는 것**이다. 이기는 순간 암기와 일반화를 구분할 수 없다.
          ⇒ C 학습 전에 반드시 고쳐야 한다. 고치는 방법은 §8.3 에 확정해 뒀다.

[방법론]  적대적 검수가 근거의 구멍 넷을 잡았고 **셋은 내 잘못이 맞았다.**
          그 중 하나(뭉갬 배율 한 점에 결론이 걸림)는 016 §3.7 이 'seed 한 쌍'으로
          겪은 실수의 재발이다. 형태만 바뀌어 같은 실수를 또 했다.
```

---

## 1. 무엇을 물었나

016 §9.2 가 잔차 노선의 천장과 바닥을 쟀다. 이득의 출처가 뜻밖이었다 — DINO 가 아니라 Video 였다.

```
  뭉갠 잔차 4배 α=1 을 배점까지 곱해 분해하면
    DINO    0.3 × (0.15664 − 0.12308) = +0.01007   ← 손해 (뭉개면 장면이 더 틀려 보인다)
    Video   0.3 × (0.02297 − 0.09110) = −0.02044   ← 이득
    Action  0.4 × (1.22386 − 1.24017) = −0.00652   ← 이득
                                        ─────────
                                         −0.01690
```

그래서 자연스러운 질문이 나왔다. **DINO 손해는 "잔차를 더하면 뭉개진다"에서 온다.
픽셀을 옮기기만 하면(워핑) 그 손해가 없어지지 않을까?**

```
  덧셈 잔차   출력 = 첫프레임 + 잔차
      단점: 모델이 만드는 잔차는 저주파라 디테일이 흐려진다 (실측 DINO +0.010)

  워핑        출력 = 첫프레임의 픽셀을 흐름대로 밀어 옮긴 것
      장점: 원본 픽셀을 그대로 옮기므로 디테일이 안 뭉개진다   ← 이 가설을 검증한다
      단점: 가려졌다 드러나는 자리를 못 채운다
```

어느 단점이 큰지는 **로봇 팔이 화면에서 차지하는 면적과 배경의 복잡도**에 달렸고,
그건 우리 데이터에만 있는 정보다. 문헌은 일반론만 준다(017 §6.3 이 확인했다).

---

## 2. 판정선을 결과 전에 고정했다 — 그리고 그 중 하나는 틀렸다

016 §7-3 의 교훈: 판정선을 결과 본 뒤에 정하면 '조금만 더' 편향이 생긴다.

| 게이트 | 비교 | 무엇을 뜻하나 |
|---|---|---|
| **0. 킬스위치** | `warp:1` vs `addblur4:1` | 흐름이 **완벽해도** 뭉갠 덧셈을 못 이기면 워핑은 표현력 자체가 부족한 것 |
| **1. 현실적 천장** (주) | `warpc4:1` vs `addblur4:1` | 둘 다 4배로 뭉갠 짝. 모델이 실제로 만들 수 있는 것끼리 |
| **2. 바닥** (보조) | `warpnbr:α` vs `addnbr:α`, α∈{0.25,0.5,1} | 방향이 틀렸을 때 누가 덜 다치나 |

```
Gate 0   Δ₀ ≥ 0 이고 t₀ ≥ +2        →  워핑 폐기. Gate 1·2 무관하게 덧셈 확정
Gate 1   Δ₁ ≤ −0.005 이고 t₁ ≤ −2   →  워핑 유리
         Δ₁ ≥ +0.005 이고 t₁ ≥ +2   →  덧셈 유리
         그 외                       →  무승부
Gate 2   세 α 전부 부호가 같아야 인정한다
최종     Gate 0 발동 → 덧셈 / Gate 1 워핑유리 & Gate 2 불리아님 → 워핑
         Gate 1 무승부 & Gate 2 워핑유리 → 워핑 / 그 외 → 덧셈(동점은 단순한 쪽)
```

**0.005 를 고른 근거 둘 중 하나는 틀렸다.** 미리 적어 두고 나중에 검증한 덕에 잡혔다.

| 근거 | 내용 | 사후 검증 |
|---|---|---|
| ① 의사결정 무차별 구간 | 덧셈 노선의 현실적 이득 전체가 −0.0169 인데, 그 30% 도 안 되는 차이로 구현 복잡도를 2배 올릴 수 없다 | **타당하다.** 통계가 아니라 선호 선언이고, 사전에 고정했으므로 정당하다. 실제로 §3.8 에서 값을 했다 |
| ② 통계적 분해능 | 짝지은 DINO 차이 SE=0.00834 → TOTAL 환산 0.3×0.00834≈0.0025, 즉 0.005≈2·SE | **틀렸다.** 실측 SE=0.00472 라 2·SE=0.00944 다. 사전 추정이 **3.8배** 빗나갔다 |

②가 틀린 이유가 교훈적이다. `SE(TOTAL) = 0.3·SE(ΔDINO)` 는 **Video 와 Action 의 표본별
차이가 정확히 0일 때만** 성립한다. 그런데 016 §9.2 는 이득의 출처가 Video 라고 스스로
밝혔다 — 즉 **가장 큰 분산 성분을 통째로 빼고** 계산한 것이다.

> **다음부터**: SE 를 사전 추정할 때는 **가장 큰 성분의 분산부터** 센다.

결론은 안 바뀌었다. 게이트가 `|Δ| ≥ 0.005` **와** `|t| ≥ 2` 를 **동시에** 요구하므로 늘
더 엄격한 쪽이 구속하고, Δ=+0.01407 > 0.00944 로 양쪽을 통과했다. **운이 좋았다.**

---

## 3. 측정 ① — 워핑 vs 덧셈

### 3.1 먼저 재현 검증 (이게 통과 안 되면 나머지는 볼 필요가 없다)

이번 측정은 016 §9.2 의 덧셈 변형들을 **같은 실행 안에서 다시 계산**한다.
표본별로 짝짓기 위해서인데, 덤으로 **독립 재현 검증**이 된다.

| 변형 | 016 §9.2 | 이번 측정 | 일치 |
|---|---:|---:|:---:|
| 정지영상 TOTAL | 0.56032 | 0.56032 | ✅ |
| 완벽한 잔차 α=1 | −0.07121 | −0.07121 | ✅ |
| 뭉갠 잔차 4배 α=1 | −0.01690 | −0.01690 | ✅ |
| 남의 잔차 α=0.25 | +0.01700 | +0.01700 | ✅ |

**소수점 5자리까지 전부 일치한다.** 새 스크립트가 기존 결과를 재현한다.

추가 새너티 둘도 통과했다.

```
  완벽한 잔차 α=1 (= 정답 영상)   DINO −0.00000   Video 0.00000     ← 0 이어야 하고 실제로 0
  워핑 α=1 픽셀 L1  6.988  <  정지영상 8.498                        ← 흐름 방향이 맞다
```

두 번째가 중요하다. 흐름 방향이 뒤집혔다면 픽셀 오차가 **커졌을** 것이다.

### 3.2 결과 (n=96, 전부 낮을수록 좋다)

| 변형 | DINO | Video | Action | TOTAL | static 대비 |
|---|---:|---:|---:|---:|---:|
| 정지영상 (α=0) | 0.12308 | 0.09110 | 1.24017 | 0.56032 | — |
| 워핑(원본흐름) α=0.25 | 0.12782 | 0.06740 | 1.24235 | 0.55551 | −0.00482 |
| 워핑(원본흐름) α=0.5 | 0.13682 | 0.06032 | 1.24099 | 0.55554 | −0.00479 |
| **워핑(원본흐름) α=1** | 0.14963 | 0.05425 | 1.23682 | 0.55589 | **−0.00443** |
| 워핑(흐름 4배 뭉갬) α=0.6 | 0.14278 | 0.05941 | 1.23946 | 0.55644 | −0.00388 |
| **워핑(흐름 4배 뭉갬) α=1** | 0.15604 | 0.05483 | 1.23560 | 0.55750 | **−0.00283** |
| 워핑(흐름 8배 뭉갬) α=1 | 0.16373 | 0.05712 | 1.23541 | 0.56042 | +0.00010 |
| 워핑(남의 흐름) α=0.25 | 0.13640 | 0.09203 | 1.24463 | 0.56638 | +0.00605 |
| 워핑(남의 흐름) α=1 | 0.16419 | 0.10257 | 1.24438 | 0.57778 | +0.01746 |
| 완벽한 잔차 α=1 (=정답) | 0.00000 | 0.00000 | 1.22277 | 0.48911 | −0.07121 |
| **덧셈(잔차 4배 뭉갬) α=1** | 0.15664 | 0.02297 | 1.22386 | 0.54343 | **−0.01690** |
| 덧셈(잔차 8배 뭉갬) α=1 | 0.16634 | 0.05167 | 1.22781 | 0.55653 | −0.00380 |
| 덧셈(남의 잔차) α=0.25 | 0.17227 | 0.10385 | 1.23622 | 0.57732 | +0.01700 |
| 덧셈(남의 잔차) α=1 | 0.25933 | 0.13187 | 1.23527 | 0.61147 | +0.05115 |
| 혼합(뭉갠워핑+뭉갠보정) | 0.14535 | 0.02011 | 1.22335 | 0.53898 | −0.02134 |
| 혼합(원본워핑+뭉갠보정) | 0.13053 | 0.01927 | 1.22314 | 0.53420 | −0.02613 |

원자료: `results/branchC/warp_vs_add.json` (표본별 rows 포함) ·
로그: `run_logs/20260801_1731_warp_oracle_n96.log`

### 3.3 게이트 판정

```
gate0_kill      warp:1   vs addblur4:1   Δ=+0.01247  SE=0.00480  t=+2.60   34/96
gate1_ceiling   warpc4:1 vs addblur4:1   Δ=+0.01407  SE=0.00472  t=+2.98   36/96
```

**Gate 0 발동.** 사전 등록한 규칙에 따라 Gate 1·2 와 무관하게 **덧셈 확정**이다.
Gate 1 도 같은 방향으로 덧셈 유리를 가리킨다.

**static 자체와의 비교가 더 인상적이다** (게이트에 안 넣었던 값):

```
  warp:0.25   vs static   Δ=−0.00482  t=−3.24   60/96   ← 유의하지만 작다
  warp:1      vs static   Δ=−0.00443  t=−1.21   58/96   ← 유의하지 않다
  warpc4:1    vs static   Δ=−0.00283  t=−0.77   57/96   ← 유의하지 않다
  addblur4:1  vs static   Δ=−0.01690  t=−3.48   61/96   ← 유의하다
```

> **현실적 워핑(뭉갠 흐름)은 아무것도 안 하는 것과 통계적으로 구별되지 않는다.**

### 3.4 왜 졌나 — 성분으로 쪼개면 한 줄이다

```
  warp:1 − addblur4:1  (음수면 워핑이 낫다)

    DINO    Δ=−0.00701   t= −0.88     배점곱 −0.00210   ← 워핑이 낫다. 그러나 유의하지 않다
    Video   Δ=+0.03128   t=+11.93     배점곱 +0.00938   ← 워핑이 크게 진다
    Action  Δ=+0.01296   t= +1.39     배점곱 +0.00518
                                      ─────────────────
                                       합계 +0.01247
```

**016 §9.2-d 의 가설은 "방향은 맞고 크기가 무의미"했다.**
워핑이 DINO 를 덜 해치는 것은 사실이지만(−0.007) 유의하지 않고, 배점을 곱하면 −0.002 다.
그런데 이득이 사는 축인 Video 에서 **t=+11.93** 으로 압도적으로 진다.

즉 **워핑은 DINO 전투에서 이기고 Video 전쟁에서 졌다.**

### 3.5 왜 졌나 — 눈으로 보면 통계보다 명확하다

`artifacts/branchC/20260801_1747_warp_vs_add_frame15.png` (표본 3개 × 프레임 15)

세 표본에서 일관되게 보이는 것:

```
  정답      팔이 위로 빠져나갔다. 원래 팔이 가리던 배경이 **드러났다**
  정지영상  팔이 처음 자리에 그대로
  워핑      팔이 거의 그대로 남아 있다  ← 여기가 핵심
  덧셈      팔이 흐릿하게 **지워지고** 배경이 부분적으로 복원됐다
```

**왜 워핑이 팔을 못 지우나.** 역방향 워핑은 "프레임 t 의 각 픽셀은 프레임 0 의 어디에서
왔는가"를 물어 그 자리의 픽셀을 당겨온다. 그런데 팔이 비켜난 자리의 배경은
**프레임 0 에서 팔에 가려져 있어 화면 어디에도 존재하지 않는다.**
흐름 추정기는 대응점이 없으니 0 에 가까운 값을 내고, 결과적으로 팔이 남는다.

문헌이 이것에 이름을 붙여 뒀다 — SDC-Net 의 **"foreground stretching"**
(017 §6.5). 그리고 이렇게 경고한다: **"backward 워핑은 구멍을 안 남긴다.
대신 조용히 틀린 픽셀을 채운다."** 우리 그림이 그 문장 그대로다.

**흐름 자체는 제대로 일한다.** 3번째 표본(집게가 테이프로 내려오는 장면)에서는
워핑이 집게를 선명하게 잘 내렸다. 문제는 흐름의 품질이 아니라 **표현의 한계**다.

### 3.6 검수가 잡은 것 — 뭉갬 배율 **한 점**에 결론이 걸려 있었다

측정을 돌리는 동안 설계를 적대적 검수에 걸었다. 가장 아픈 지적:

> 사전등록은 k=4 **한 점만** 게이트로 걸었다. 스크립트는 k=8 도 계산해 놓고 게이트에서 뺐다.
> 저장된 rows 로 사후 계산하면:
>
> ```
>   warpc4 vs addblur4   Δ=+0.01407  t=+2.98   덧셈 유의하게 유리
>   warpc8 vs addblur8   Δ=+0.00389  t=+0.98   무승부
> ```
>
> 부호는 같은데 크기가 3.6배, 유의성이 사라진다.

**016 §3.7 이 'seed 한 쌍'으로 겪은 실수가 '배율 한 점'으로 재발했다.**
형태만 바뀌었을 뿐 같은 실수다. 이틀 만에 세 번째다.

그 밖에 인정한 지적:

| 지적 | 내용 | 처분 |
|---|---|---|
| Gate 2 가 구조적으로 편향됐다 | `warpnbr`(내 픽셀을 틀린 방향으로) 과 `addnbr`(**생판 다른 방의 델타를 통째로 이식**)은 틀림의 급이 다르다. 이웃을 행동 거리로만 골라 장면이 완전히 다르기 때문이다 | **Gate 2 폐기.** 공정한 바닥을 새로 잰다(§3.7) |
| 판정이 TOTAL 위에 있다 | 016 §5 가 **로컬 Action 은 폐기 대상**이라 못 박았는데 게이트의 40% 가 그것이다 | **DV(=0.3D+0.3V)를 병기**한다. 검수가 재계산한 DV 기준으로도 결론 유지(gate1 DV Δ=+0.00938, t=+3.36 — 오히려 t 가 커진다) |
| 세 α 는 독립 반복이 아니다 | 하나의 장을 스칼라배 한 것이라 부호 일치가 거의 공짜다. 정보량이 사실상 1 | 맞다. 진짜 독립 축은 **뭉갬 배율·흐름 추정기·이웃 정의**다 |
| 새너티가 약속만 되고 실행 안 됨 | 독스트링은 "α=0 → 정확히 static"을 내세웠는데 변형 목록에 없었다 | `warp:0` 추가(§3.7) |
| "완벽한 흐름"이 전제인데 RAFT 추정치다 | 누적 변위 p99 55px 는 RAFT 가 편한 구간이 아니다 | **프레임별 DINO** 로 흐름 품질과 가림/드러남을 분리(§3.7) |

**다만 한 가지는 반박한다.** "RAFT 가 부정확해서 워핑을 억울하게 죽인 것 아닌가"에 대해:

> 우리 모델은 흐름을 **정답 영상 없이, 첫 프레임과 행동만으로 예측**해야 한다.
> 두 프레임을 다 보는 전용 광류 네트워크(RAFT)가 55픽셀 변위를 못 푼다면,
> 한 프레임만 보는 우리 모델은 더 못 푼다. **이 교란은 워핑에 불리한 쪽으로 작용한다.**

### 3.7 2라운드 — 구멍을 메운 결과

지적을 반영해 다시 쟀다(n=96). 로그: `run_logs/20260801_1800_warp_round2_n96.log` ·
원자료: `results/branchC/warp_round2_raft.json`

| 변형 | DINO | Video | Action | **DV** | TOTAL | **ΔDV** | ΔTOTAL |
|---|---:|---:|---:|---:|---:|---:|---:|
| 정지영상 | 0.12308 | 0.09110 | 1.24017 | 0.06425 | 0.56032 | — | — |
| staticexact (첫프레임 16복사) | 0.12313 | 0.09110 | 1.24105 | 0.06427 | 0.56069 | +0.00002 | +0.00037 |
| **warp:0** (새너티) | 0.12314 | 0.09110 | 1.24105 | 0.06427 | 0.56069 | +0.00002 | +0.00037 |
| 워핑 흐름 2배 뭉갬 | 0.15082 | 0.05454 | 1.23676 | 0.06161 | 0.55631 | −0.00265 | −0.00401 |
| 워핑 흐름 4배 뭉갬 | 0.15604 | 0.05483 | 1.23560 | 0.06326 | 0.55750 | −0.00099 | −0.00283 |
| 워핑 흐름 8배 뭉갬 | 0.16373 | 0.05712 | 1.23541 | 0.06625 | 0.56042 | +0.00200 | +0.00010 |
| 워핑 흐름 16배 뭉갬 | 0.17411 | 0.05998 | 1.23614 | 0.07023 | 0.56468 | +0.00597 | +0.00436 |
| 워핑 원본 흐름 | 0.14963 | 0.05425 | 1.23682 | 0.06116 | 0.55589 | −0.00309 | −0.00443 |
| **덧셈 잔차 2배 뭉갬** | **0.10271** | **0.00809** | 1.22276 | **0.03324** | **0.52234** | **−0.03101** | **−0.03798** |
| 덧셈 잔차 4배 뭉갬 | 0.15664 | 0.02297 | 1.22386 | 0.05388 | 0.54343 | −0.01037 | −0.01690 |
| 덧셈 잔차 8배 뭉갬 | 0.16634 | 0.05167 | 1.22781 | 0.06540 | 0.55653 | +0.00115 | −0.00380 |
| 덧셈 잔차 16배 뭉갬 | 0.15486 | 0.07926 | 1.22424 | 0.07024 | 0.55993 | +0.00598 | −0.00039 |
| 완벽한 잔차 (=정답) | 0.00000 | 0.00000 | 1.22277 | 0.00000 | 0.48911 | −0.06425 | −0.07121 |
| 워핑(남의 흐름) | 0.16419 | 0.10257 | 1.24438 | 0.08003 | 0.57778 | +0.01577 | +0.01746 |
| **덧셈(남의 흐름을 덧셈으로)** | 0.15112 | 0.09996 | 1.24688 | 0.07532 | 0.57408 | +0.01107 | +0.01376 |
| 덧셈(남의 잔차) — 구 바닥 | 0.25933 | 0.13187 | 1.23527 | 0.11736 | 0.61147 | +0.05311 | +0.05115 |
| 혼합(뭉갠워핑+뭉갠보정) | 0.14535 | 0.02011 | 1.22335 | 0.04964 | 0.53898 | −0.01462 | −0.02134 |

```
짝지은 비교 (Δ<0 이면 왼쪽이 좋다)             ΔDV       t      ΔTOTAL      t     승률
  k2 : warpc2  vs addblur2       +0.02837   +9.37    +0.03397   +6.71   20/96
  k4 : warpc4  vs addblur4       +0.00938   +3.36    +0.01407   +2.98   36/96
  k8 : warpc8  vs addblur8       +0.00085   +0.45    +0.00389   +0.98   47/96
  k16: warpc16 vs addblur16      −0.00001   −0.01    +0.00475   +1.15   46/96

  공정 바닥 : warpnbr vs addnbrwarp4   +0.00470  +4.16   +0.00370   +2.54   30/96
  구 바닥(무효): warpnbr vs addnbr      −0.03733 −10.83   −0.03369   −5.45   75/96
  혼합 vs addblur4                     −0.00424   −2.58   −0.00445   −2.52   65/96
```

#### ① 뭉갬 배율 네 점 — 부호는 전부 같다. 검수의 우려는 해소된다

**네 배율 전부 ΔTOTAL ≥ 0 이다. 덧셈이 지는 배율이 하나도 없다.**

k=8·16 에서 무승부가 나온 이유가 표에 그대로 보인다 — **그 지점에서는 덧셈도 이미 죽었다.**

```
  덧셈이 static 대비 얻는 이득     k2 −0.03798   k4 −0.01690   k8 −0.00380   k16 −0.00039
  워핑이 static 대비 얻는 이득     k2 −0.00401   k4 −0.00283   k8 +0.00010   k16 +0.00436
```

k=8 에서 둘 다 0 근처이니 무승부가 나오는 것은 당연하다. 그건 워핑의 선전이 아니라
**둘 다 쓸모없어진 지점**이다. 그리고 해상도가 좋아질수록 **덧셈만 좋아진다.**

> **검수의 방법론 지적은 옳고(배율 한 점에 걸지 말 것), 그 지적을 반영한 결과
> 결론은 오히려 강화됐다.**

#### ② ⚠ 공정한 바닥에서 **부호가 뒤집혔다** — 검수가 옳았다

같은 틀린 움직임을 덧셈 표현으로 바꿔 얹은 것과 비교하면:

```
  구 바닥(무효):  워핑이 덧셈보다 −0.03369 **낫다**   ← "워핑은 틀려도 덜 다친다"
  공정 바닥:      워핑이 덧셈보다 +0.00370 **나쁘다**  (t=+2.54, DV 기준 t=+4.16)
```

**"워핑은 방향이 틀려도 덜 다친다"는 주장은 폐기한다.** 그것은 매개화의 산물이었다 —
`addnbr` 은 *생판 다른 방의 델타를 통째로 이식*하는 것이고 `warpnbr` 은 *내 픽셀을
틀린 방향으로 조금 미는 것*이라, 애초에 틀림의 급이 달랐다.

**⇒ 워핑은 천장에서도 지고 바닥에서도 진다.** 016 §9.2-c 의 위험구조 논리로도 워핑을
변호할 수 없게 됐다.

#### ③ 왜 워핑이 졌나 — 흐름 품질이 아니다 (검수의 최대 우려 해소)

검수의 가장 날카로운 지적은 이것이었다: *"warp:1 은 오라클이 아니라 RAFT 추정치다.
누적 변위 p99 60px 는 RAFT 가 편한 구간이 아니다. 이 측정은 **'워핑이 우리 데이터에 안 맞는다'와
'RAFT 가 2.7초 변위를 못 푼다'를 구분하지 못한다.**"*

프레임별로 재서 갈랐다.

```
  프레임별 픽셀 L1 (워핑 − 정지영상). 음수여야 흐름이 일한 것이다.
    t          0        3        6        9       12       15
    차이   +0.000   −1.700   −1.743   −1.726   −1.485   −1.462

  프레임별 DINO 거리 (정지영상 대비 차이)
    t          0        3        6        9       12       15
    static  0.0036   0.0768   0.1175   0.1504   0.1727   0.1918
    warp:1  +0.0000  +0.0183  +0.0323  +0.0395  +0.0333  +0.0315
    addblur4 −0.0008 +0.0496  +0.0544  +0.0329  +0.0231  +0.0130
```

**읽는 법**: 변위는 t 에 비례해 커진다(t=15 가 t=3 의 약 5배). 흐름 품질이 범인이라면
**t 가 커질수록 픽셀 L1 이득이 0 쪽으로 사그라들어야 한다.**

**그러지 않는다.** −1.700 → −1.462, 14% 줄었을 뿐이고 끝까지 확실히 음수다.
**즉 RAFT 는 마지막 프레임까지 제 몫을 한다.** 그런데도 DINO 손해는 남는다.

> **⇒ 워핑이 진 것은 흐름이 부정확해서가 아니라 표현의 구조적 한계 때문이다.**
> 픽셀 오차는 줄이면서 특징 거리는 못 줄인다 — §3.5 의 그림이 그 이유다
> (드러난 배경을 못 만들어 팔이 복제된다).

덤으로 보이는 것: **덧셈의 DINO 손해는 t 가 커질수록 줄어든다**(+0.0496 → +0.0130).
초반에는 바뀐 게 적어 뭉갬의 대가가 상대적으로 크지만, 후반에는 실제로 새 내용을
만들어낸 값어치가 커진다. **긴 지평일수록 덧셈이 유리하다.**

#### ④ 새너티 — 좌표 규약은 맞다

```
  warp:0 vs staticexact   최대 성분 차이 7.74e-06
  add:1  의 DINO −1.96e-10, Video 4.12e-09   (정답과 같아야 하므로 0)
```

`warp:0` 은 내가 건 문턱(1e-6)을 아슬아슬하게 넘겨 "실패"로 찍혔지만 **실질은 통과다.**
별도로 확인했다 — 무작위 영상에 α=0 워핑을 걸면 **uint8 변환 후 다른 픽셀이 0개**다.
7.74e-06 은 `grid_sample` 의 float32 정밀도이며, 우리가 재는 효과(1e-2)보다 **여섯 자릿수** 작다.

> **문턱이 잘못됐다.** float32 를 통과하는 값에 1e-6 을 요구한 것이 무리였다.
> 다음부터 이런 새너티는 **uint8 픽셀 일치**로 걸어야 한다.

#### ⑤ ⚠ 그리고 예상 못 한 것 — 016 의 헤드룸 추정이 2.2배 낮게 잡혀 있었다

016 §9.2 는 뭉갬 배율을 **4배와 8배만** 재고 "잔차 노선의 현실적 이득 = −0.01690" 이라 적었다.
**2배를 재니 −0.03798 이다. 2.2배 크다.**

더 중요한 것은 **성분 분해가 뒤집힌다는 것**이다.

```
  016 §9.2 ② (k=4 기준)                    이번 (k=2 기준)
    DINO   +0.01007  손해                    DINO   0.3×(0.10271−0.12308) = −0.00611  이득
    Video  −0.02044  이득                    Video  0.3×(0.00809−0.09110) = −0.02490  이득
    Action −0.00652  이득                    Action 0.4×(1.22276−1.24017) = −0.00696  이득
           ─────────                                ─────────
            −0.01690                                 −0.03798
```

016 은 이렇게 결론지었다:

> 잔차 노선의 정체는 "장면을 정확히 그린다"가 아니라 **"대충이라도 움직여 Video 를 챙기고
> DINO 손해를 감수한다"** 이다. **DINO 를 지키려 애쓰는 설계는 방향이 틀린 것일 수 있다.**

**이 문장은 k=4 의 산물이었다. k=2 에서는 세 성분이 전부 이득이고 DINO 도 static 을 이긴다**
(0.10271 < 0.12308). **⇒ 016 §9.2 ② 의 설계 함의는 여기서 철회한다.**

새 그림은 훨씬 단순하고 희망적이다.

```
  잔차의 유효 해상도       static 대비 이득
    1/2                    −0.03798     ← DINO 도 이득
    1/4                    −0.01690     ← 016 이 유일하게 잰 점. DINO 손해
    1/8                    −0.00380
    1/16                   −0.00039
    완벽(정답)             −0.07121     (도달 불가)
```

> **해상도가 곧 이득이다.** 그리고 §4.3 의 흐림 예산(1/2 이상)과 정확히 같은 지점을 가리킨다.
> 두 측정이 독립적으로 같은 답을 냈다.

### 3.8 혼합(워핑 + 덧셈 보정)은 어떻게 되나

**문헌이 예측한 대로 혼합이 가장 좋다.** 017 §6.1 이 정리했듯 RIFE·EMA-VFI 의 출력식이
문자 그대로 `워핑 + 마스크 + 유계 잔차`다.

```
  warpc4+res4:1  vs addblur4:1   Δ=−0.00445  t=−2.52   65/96
  warp+res4:1    vs addblur4:1   Δ=−0.00923  t=−4.95   73/96
```

**유의하게 이긴다.** 그런데 채택하지 않는다. 이유가 둘이다.

1. **`warp+res4` 는 도달 불가능하다.** 원본 해상도 오라클 흐름을 쓴다. 뭉갠 흐름을 쓰는
   현실적 변형은 `warpc4+res4` 이고 그 이득은 **−0.00445 로 무차별 구간(0.005) 안**이다.
2. **사전 등록한 무차별 규칙이 그대로 적용된다.** 0.005 를 못 넘는 이득으로
   흐름 헤드 + 미분가능 워핑 + 경계 처리 + 항등 붕괴 방어를 추가할 수 없다. 19일뿐이다.

> **그러나 묻어 두지 않는다.** 혼합은 **DINO 축에서 유의하게 이긴다**(Δ=−0.01129, t=−2.21).
> DINO 는 우리가 로컬 −0.069, eval −31% 를 줄여야 하는 바로 그 축이다.
> **DINO 가 병목으로 확정되면 되돌아올 1순위 후보다.**
>
> 되돌아올 때의 설계도 이미 있다(017 §6.5): **흐름 헤드를 zero-init** 하면
> step 0 에서 항등 워핑이라 출력이 정확히 static 이고, 흐름이 끝내 안 배워지면
> **그대로 순수 덧셈 모델로 퇴화한다.** 즉 혼합의 실패 모드가 곧 덧셈 모델이다.
> 하방이 막혀 있다는 점에서 016 §9.2-c 의 논리가 그대로 성립한다.

---

## 4. 측정 ② — 흐림 세금: 예산이 σ ≈ 1.3픽셀뿐이다

### 4.1 왜 쟀나

우리가 가려는 결정론 회귀는 **필연적으로 흐릿한 영상**을 낸다. 여러 그럴듯한 미래의
한가운데를 찍기 때문이다(017 §3). 그러면 얼마나 흐려도 되나?

문헌은 "DINOv2 는 흐림에 **비교적** 관대하다"고 말하지만(DreamSim, DINO-IR),
동시에 **약한 흐림에서만** 그렇다고도 한다([arXiv:2401.00463](https://arxiv.org/abs/2401.00463)).
그리고 결정적으로 **우리가 서 있는 지점이 문헌의 지점과 다르다.**

```
  분류 문제의 감각:  코사인 유사도 0.85 면 "확실히 비슷하다"
                    (서로 다른 이미지 쌍이 0.3~0.5 이므로)

  우리의 작동 범위:  같은 장면 이웃 프레임끼리 0.99 근처
                    여기서 0.98 → 0.90 으로 떨어지면 거리는 0.02 → 0.10, **5배**
```

**남의 감각을 옮겨 쓰면 크게 틀린다. 그래서 직접 쟀다.**

### 4.2 결과 (n=96)

정답 영상에 흐림을 걸어 **정답 자신과** 채점한 것이다. 즉 순수한 흐림의 비용이다.

| 변형 | DINO | Video | Action | TOTAL | 정답 대비 | static 대비 |
|---|---:|---:|---:|---:|---:|---:|
| 정답 영상 | 0.00000 | 0.00000 | 1.22277 | 0.48911 | — | −0.07121 |
| **정지영상** | 0.12308 | 0.09110 | 1.24017 | 0.56032 | +0.07121 | — |
| 흐림 σ=0.5px | 0.00193 | 0.00156 | 1.22247 | 0.49003 | **+0.00092** | −0.07029 |
| **흐림 σ=1px** | 0.02080 | 0.01319 | 1.22160 | 0.49884 | **+0.00973** | −0.06149 |
| **흐림 σ=2px** | 0.09436 | 0.04958 | 1.22101 | 0.53159 | **+0.04248** | −0.02874 |
| 흐림 σ=4px | 0.31890 | 0.18517 | 1.21353 | 0.63663 | +0.14752 | +0.07631 |
| 흐림 σ=8px | 0.56551 | 0.43357 | 1.18878 | 0.77523 | +0.28612 | +0.21491 |
| 해상도 1/2 | 0.03174 | 0.01305 | 1.22099 | 0.50183 | **+0.01272** | −0.05849 |
| 해상도 1/4 | 0.13915 | 0.04634 | 1.22140 | 0.54421 | **+0.05510** | −0.01612 |
| 해상도 1/8 | 0.40121 | 0.16332 | 1.21505 | 0.65538 | +0.16627 | +0.09506 |
| 해상도 1/16 | 0.63802 | 0.36296 | 1.19204 | 0.77711 | +0.22800 | +0.21679 |

원자료: `results/branchC/blur_and_averaging.json` ·
로그: `run_logs/20260801_1745_blur_and_averaging_n96.log`

### 4.3 판정 (판정선은 결과 전에 017 §8.4 에 고정했다)

```
  눈금:  잔차 노선의 현실적 이득 전체가 −0.01690 이다.
        흐림 세금이 이보다 크면 흐림으로 얻은 이득이 통째로 사라진다.

  세금이 0.017 을 넘는 최소 σ 를 σ* 라 하면
    σ=1px  +0.00973   아직 여유
    σ=2px  +0.04248   이미 2.5배 초과
    ⇒ **σ* ≈ 1.3픽셀**

  사전 등록한 해석:  σ* ≥ 4 → 관대 / 1 < σ* < 4 → 중간 / σ* ≤ 1 → 가혹
  ⇒ **"중간", 그것도 가혹한 쪽 끝이다.**
```

해상도로 환산하면:

```
  1/2  +0.01272   여유
  1/4  +0.05510   3.2배 초과
  ⇒ 모델 출력의 **유효 해상도는 320×512 의 1/2 이상**이어야 한다 (대략 160×256)
```

### 4.4 ⚠ 이게 C 설계에 직접 걸린다

017 §7.2 는 문헌을 근거로 "우리 규모로 충분하다"며 **128×128** 설계
(Interactive World Simulator, RSS 2026)를 인용했다. **우리 채점에는 너무 작다.**

```
  채점 공간 320×512 (내용은 320×427, 나머지는 검은 띠)
  128×128 모델의 유효 해상도 = 세로 128/320 ≈ 1/2.5   ← 예산 1/2 미달
```

**⇒ C 의 출력 해상도는 최소 160×256, 안전하게는 채점 해상도 그대로 320×512 로 간다.**

### 4.5 그리고 §3.7⑤ 가 **독립적으로 같은 답**을 냈다

이 절(§4)은 **정답 영상 전체를 흐리게** 해서 잰 것이고,
§3.7⑤ 는 **잔차만 뭉개서** 잰 것이다. 다른 측정인데 같은 지점을 가리킨다.

| 유효 해상도 | 정답 전체를 뭉갤 때 (§4.2) | **잔차만 뭉갤 때** (§3.7⑤) |
|---|---:|---:|
| 1/2 | −0.05849 | **−0.03798** |
| 1/4 | −0.01612 | −0.01690 |
| 1/8 | +0.09506 | −0.00380 |
| 1/16 | +0.21679 | −0.00039 |

```
  1/2~1/4 구간에서는 두 방식이 비슷하다
  1/8 이하에서 **완전히 갈린다** — 전체를 뭉개면 재앙(+0.095)이고 잔차만 뭉개면 그냥 무이득(−0.004)
```

**이게 잔차 표현의 진짜 이점을 보여준다.** 잔차는 대부분이 0 이라, 해상도가 모자라도
**정지 배경이 안 망가진다.** 최악이 "아무 이득 없음"이지 "재앙"이 아니다.
016 §9.2-c 가 말한 위험 구조가 해상도 축에서도 성립한다.

> **⇒ 해상도는 이득의 크기를 정하고, 잔차 표현은 하방을 막는다. 둘 다 필요하다.**

### 4.6 ⚠ 정직하게 — 잔차 표현은 공짜가 아니다

같은 1/2 해상도에서 두 방식을 나란히 놓으면 잔차가 **진다.**

| 1/2 해상도에서 | DINO | Video | TOTAL | static 대비 |
|---|---:|---:|---:|---:|
| 정답 전체를 1/2 로 (`coarse:2`) | **0.03174** | **0.01305** | 0.50183 | **−0.05849** |
| static + 1/2 잔차 (`addblur2`) | 0.10271 | 0.00809 | 0.52234 | −0.03798 |

DINO 에서 3배 넘게 차이 난다(0.032 vs 0.103). 이유는 이렇다.

```
  정답을 1/2 로 뭉갠 것    : 전체가 흐리지만 **내용이 전부 맞다**
  static + 1/2 잔차        : 안 움직인 곳은 **선명하고 정확**하지만
                            움직인 곳은 **낡은 배경 위에 흐릿한 보정**이 얹힌 상태
```

DINOv2 는 "고르게 흐리지만 맞는 그림"을 "부분적으로 선명하지만 부분적으로 낡은 그림"보다
훨씬 좋아한다.

**그런데 이 비교로 설계를 정하면 안 된다.** 둘 다 오라클이라 **모델이 무엇을 배워야 하는지**를
전혀 반영하지 않기 때문이다.

```
  프레임 전체를 예측하는 모델 : 배경까지 **다시 그려야** 한다. 실패하면 쓰레기 영상
  잔차를 예측하는 모델        : 배경은 **그냥 흘려보낸다**. 실패하면 정확히 static
```

016 §9.2-c 가 잔차를 고른 이유가 성능이 아니라 위험 구조였다는 것을 다시 확인한다.
**이 표는 "잔차가 더 좋은 점수를 낸다"는 주장을 여전히 지지하지 않는다.**
지지하는 것은 **"잔차는 더 못해질 수가 없다"** 뿐이다.

> **설계서에서 다룰 것**: 잔차 표현의 이 DINO 손해(−0.02 어치)를 줄일 방법이 있는가.
> 후보 하나는 **잔차를 더할 때 배경 쪽도 살짝 함께 흐리는 것**(전역 일관성 확보)인데,
> 이건 §4.3 의 흐림 예산(σ ≤ 1.3px)과 정면으로 부딪힌다. 재 봐야 안다.

---

## 5. 측정 ③ — 평균내기 대리실험: 실패했다. 그러나 질문에는 답하지 못했다

### 5.1 왜 쟀나

017 §4 에서 찾은 것이다. 우리와 거의 같은 대회(1X World Model Challenge)의 우승팀이
확산모델을 버리지 않고 **샘플 20개를 픽셀공간에서 평균**내 PSNR +2.25dB 를 얻었다.

우리 1.1B 는 버릴 자산이 아니라 **평균 추정기로 재활용할 자산**일 수 있다.
다만 진짜 실험은 GPU 몇 시간이 든다. 그 전에 모델 없이 값싸게 물었다.

> **"그럴듯하지만 틀린 움직임" 여러 개를 평균내면 하나보다 나은가?**
> (표본 i 의 예측 = 첫프레임 + 행동이 비슷한 다른 표본 K개의 잔차 평균)

### 5.2 결과 — 오히려 나빠졌다

```
평균내기가 정말 이득인가 (Δ<0 이면 K개 평균이 낫다)
  avg2 vs avg1 @α=1     Δ=+0.00732  t=+1.57   45/96
  avg4 vs avg1 @α=1     Δ=+0.01558  t=+2.35   42/96
  avg8 vs avg1 @α=1     Δ=+0.01012  t=+1.53   38/96
  avg2 vs avg1 @α=0.5   Δ=+0.00493  t=+1.30   45/96
  avg4 vs avg1 @α=0.5   Δ=+0.00906  t=+1.71   37/96
  avg8 vs avg1 @α=0.5   Δ=−0.00157  t=−0.32   45/96
```

**단조 감소가 없다.** 사전 등록한 규칙(017 §8.4-D)에 따라
**"실제 K개 생성 실험의 우선순위를 낮춘다. 폐기는 아니다."**

### 5.3 왜 폐기가 아닌가 — 대리값이 질문에 안 맞는다

이 한계는 **측정 전에** 적어 뒀다(017 §8.3).

```
  남의 잔차 : **체계적으로** 틀리다(다른 방, 다른 물체).
              K개를 평균내면 K개의 다른 장면이 겹칠 뿐이고, 그건 더 틀린 것이다.

  확산 샘플 : 같은 조건에서 **무작위로** 흔들린다.
              평균이 조건부 평균으로 수렴한다. 이게 1X 우승팀이 얻은 것이다.
```

**구조가 다르다.** 그래서 이 실패로 실제 실험을 접을 수 없다.
**진짜 질문은 이것이다 — 우리 모델의 오차는 흔들림(분산)인가 치우침(편향)인가?**

```
  분산이면  →  샘플마다 다른 방향으로 틀린다.  평균이 고친다
  편향이면  →  일관되게 한 방향으로 틀린다.    평균은 아무것도 못 한다
```

**오늘 밤 이걸 잰다** (§9).

---

## 6. 부수 발견 넷

### 6.1 커널 기반 변환(CDNA/SepConv 계열)은 배제된다

017 §6.4 가 "커널 기반은 워핑과 덧셈 사이의 절충이고 로봇 조작을 위해 설계됐다"며
후보로 올렸다. 우리 데이터가 그것을 배제한다.

```
  누적 변위(프레임 0 대비, 레터박스와 t=0 을 빼고 잰 값, n=96 평균)
      p95 24.0px      p99 59.9px      max 96.8px        (화면 폭이 512px 다)

  커널 기반은 **커널 크기 ≥ 2 × 변위**가 필요하다  →  120px 이상
  SepConv 이 쓰는 커널이 51×51 이다. 자릿수가 다르다.
```

※ 1라운드는 t=0 프레임(흐름이 정확히 0)과 레터박스 검은 띠를 포함해 백분위를 재서
**p99 를 55px 로 과소평가**했다(전체의 약 22% 가 0 이라 희석됐다). 검수 지적을 받아 고쳤다.

**한 프레임에서 16프레임을 한 번에 내는 구조에서는 커널 기반이 불가능하다.**
(프레임을 하나씩 이어붙이는 자기회귀라면 프레임당 변위가 1/15 로 줄어 이야기가 달라진다.
다만 그건 오차 누적을 부른다 — 017 §5.1⑦.)

### 6.2 libx264 는 완전한 정지영상조차 흔든다

`warp:0` 새너티를 만들다 발견했다. 정지영상 mp4 는 **프레임 0 을 16번 복사**해 만든 것인데,
디코드하면 프레임끼리 다르다.

```
  프레임 t 와 프레임 0 의 차이:  다른 픽셀 1.95%   최대 7단계   평균 0.026
  이 잡음만으로 생기는 DINO 거리: 최대 1.8e-04
```

크기(1e-4)는 우리가 재는 효과(1e-2)보다 **두 자릿수 작아서 어떤 결론도 위협하지 않는다.**

**그리고 2라운드에서 이 잡음의 부호까지 쟀다.**

```
  static (mp4 왕복, 16프레임)  −  staticexact (첫 프레임 16번 복사)
      DINO −0.00005   Video −0.00000   Action −0.00088   TOTAL −0.00037
```

**음수 = 인코딩 잡음이 있는 쪽이 오히려 근소하게 좋다.** 거의 전부 Action 에서 나온다
(−0.00088). 완전히 멈춘 영상보다 미세하게 떨리는 영상에서 action extractor 가
행동을 조금 더 잘 읽는 것으로 보인다.

> **⇒ "인코딩이 점수를 깎으니 무손실로 바꾸면 공짜 점수"라는 기대는 성립하지 않는다.**
> 적어도 **프레임 간 잡음** 축에서는 그렇다.
>
> 다만 이건 **절대 압축 손실이 아니다.** `staticexact` 도 디코드된 mp4 의 프레임 0 을
> 쓰므로 원본 PNG 대비 손실은 이미 안고 있다. 016 §8.1 이 잰 "정지영상 프레임 0 의
> DINO 오차 0.0036" 이 그 값이고, 그건 여전히 미측정 영역이다.
> 학습 손실은 인코딩 **전** 텐서로 계산되는데 채점은 인코딩 **후** 픽셀로 이뤄진다 —
> 이 틈은 **오늘 밤 K샘플 측정이 실제 생성물로 잰다**(017 §5.4).

### 6.3 홀드아웃이 학습 데이터와 겹친다 — 이제 코드로 확정했다

016 §11 이 구조적으로 지적했던 것을 정확히 확인했다.

**학습 데이터모듈의 분할 규칙** (`ldwma/datasets/lerobot_so100.py:270-285`):

```python
examples = 모든 데이터셋의 모든 에피소드 (길이 ≥ traj_len × downsample)
self.rng.shuffle(examples)                       # rng = random.Random(seed), seed 기본 0
val_count = int(len(examples) * val_fraction)    # val_fraction = 0.05
train split = examples[val_count:]               # 95%
val   split = examples[:val_count]               #  5%
```

**우리 홀드아웃 생성기** (`scripts/build_holdout.py` → `wm_eval/data_utils.iter_holdout_samples`):

```python
eps = list_episodes(train_root, ds, min_length=traj_len)   # ← 전체 에피소드. 배제 없음
```

**⇒ 홀드아웃 96개 중 약 95% 가 학습에 쓰인 에피소드다.**

지금은 무해하다 — 결론이 전부 "학습 데이터에서조차 진다"는 보수적 방향이기 때문이다.
**그러나 C 는 이기려고 만드는 것이다.** 이기는 순간 암기와 일반화를 구분할 수 없게 된다.
고치는 방법은 §8.3 에 확정해 뒀다.

### 6.4 우리 생성 설정은 이미 1X 의 권고를 따르고 있었다

017 §4 가 "CFG 를 켜면 거리 지표가 나빠진다(−0.68dB)"고 전했는데,
확인해 보니 `scripts/branchB/configs/eval/gen_1p1b.yaml` 이 이미
`unconditional_guidance_scale: 1.0`(=CFG 끔), `ddim_eta: 0.0`(결정론) 이다.

**즉 그 이득은 이미 우리 것이다.** 남은 것은 샘플 평균뿐이고, 샘플이 달라지는
유일한 원천은 **초기 노이즈**다.

---

## 7. 결론의 경계선 — 무엇을 말할 수 있고 무엇을 말할 수 없나

**말할 수 있는 것**

> 우리 채점기·우리 홀드아웃 96표본에서, **첫 프레임을 정답 유래 흐름으로 미는 것은
> 저역통과 덧셈 잔차보다 나은 표현이 아니다.** 그 이유는 흐름 품질이 아니라
> 표현의 구조적 한계(드러난 배경을 만들 수 없음)로 보이며, 그림에서 육안으로 확인된다.

**말할 수 없는 것**

- "워핑이 원리적으로 열등하다" — 아니다. 문헌은 `워핑 + 보정` 조합이 표준이라 말하고,
  우리 측정도 혼합이 순수 덧셈을 이긴다고 말한다(§3.8).
- "덧셈이 static 을 이길 것이다" — 오라클이 −0.0169 라는 뜻이지 **모델이 그걸 낸다는 뜻이 아니다.**
  우리 확산모델의 실력은 016 §9.2 기준 "남의 움직임 35% 세기"다.
- "eval 에서도 그럴 것이다" — 016 §5 가 로컬과 eval 이 **방향까지** 어긋난 전례를 남겼다.
- **모델이 잔차를 얼마나 잘 예측할지** — 이 측정이 답하지 못하는 가장 중요한 것이다.
  오라클 잔차는 정답에서 뽑은 것이다. 모델은 (첫프레임, 행동 16단계)만 보고 만들어야 한다.
  016 §9.2 기준 우리 확산모델의 실력은 "남의 움직임 35% 세기"이고, 그 지점은
  공정 바닥(§3.7②)에서 **손해 구간**이다. **잔차 노선이 성공한다는 근거는 아직 없다.**

**답한 것 (검수가 우려했던 것)**

- 흐름 오차와 가림/드러남 중 진범 → **가림/드러남이다.** §3.7③ 이 프레임별로 갈랐다.
- 뭉갬 배율 한 점에 걸린 결론 → **네 배율 전부 같은 부호.** §3.7①
- 바닥 비교의 편향 → **공정하게 재니 부호가 뒤집혔고, 워핑에 더 불리해졌다.** §3.7②

**그리고 이건 오라클이다. 반증에는 쓰고 검증에는 쓰지 않는다.**

---

## 8. C 설계에 넘기는 것

### 8.1 확정된 것

| # | 결정 | 근거 |
|---|---|---|
| 1 | **덧셈 잔차** (출력 = 첫프레임 + 잔차). 순수 워핑 배제 — 천장에서도 바닥에서도 진다 | §3.3, §3.7① §3.7② |
| 2 | 잔차 헤드를 **zero-init** → 학습 0스텝에서 출력이 정확히 static = 리더보드 0.30325 | 016 §9.3, 017 §6.5 |
| 3 | **출력 유효 해상도 ≥ 320×512 의 1/2** (약 160×256). 128×128 설계는 안 된다 | §4.3, §4.5, §3.7⑤ — **독립 측정 셋이 같은 지점** |
| 4 | **흐림 예산 σ ≤ 1.3픽셀** (채점 해상도 기준). 설계서에 수치로 명시한다 | §4.3 |
| 5 | 행동 주입은 **per-frame FiLM 또는 AdaLN**. cross-attention 금지 | 017 §7.1 (Nano WM: −2.4dB) |
| 6 | **x-예측 계열의 결정론 회귀**. ε-예측 아님 | 017 §7.1 |
| 7 | 16프레임을 **한 번에**. 자기회귀 아님 | 017 §5.1⑦, §6.1 |
| 8 | 커널 기반 변환 배제 (변위 p99 60px → 커널 120px 필요) | §6.1 |
| 9 | **DINO 를 포기하지 않는다.** 016 §9.2 ②의 반대 함의는 철회됐다 | §3.7⑤ (k=2 에서 DINO 도 이득) |

**해상도가 왜 1순위 설계 변수가 됐는지** 한 표로:

```
  잔차의 유효 해상도    static 대비 이득     비고
    1/2                 −0.03798           DINO 도 이득 (0.10271 < static 0.12308)
    1/4                 −0.01690           016 이 유일하게 잰 점. DINO 는 손해
    1/8                 −0.00380           사실상 무이득
    1/16                −0.00039           없는 것과 같다
    완벽(정답)          −0.07121           도달 불가

  ⇒ 1/2 → 1/4 로 한 단계 잃으면 **이득의 55% 가 날아간다.**
    손실함수나 백본을 고르는 것보다 이 축이 먼저다.
```

### 8.2 손실함수 — 채점 100% 를 직접 겨냥할 수 있다

017 §5 에서 확인한 것이다. **채점 3성분이 전부 미분 가능하고, 동결한 채 손실로 쓰는 것은
규정 위반이 아니다**(§4.1-4 는 *변경*만 금지한다).

```
  L = L_pixel(L1)  +  λ_d · L_DINO(1 − cos, CLS 384차원)  +  λ_a · L_action(MAE)
```

**반드시 지킬 것 셋** (전부 017 §5.3~5.5 의 근거가 있다):

1. **픽셀 손실을 버리면 안 된다.** 의미 잠재공간만으로 학습하면 행동 제어성이
   0으로 붕괴한 보고가 있다(Nano WM Finding #4: 성공률 25% → 0%).
2. **λ 는 값이 아니라 gradient norm 정렬로 잡는다.** 세 항의 gradient norm 과
   상호 코사인 유사도를 매 스텝 로깅한다. 음의 코사인이 지속되면 PCGrad 를 넣는다.
3. **홀드아웃에서 세 성분을 개별 추적한다.** 총점만 보면 "한 항이 오르고 두 항이 내리는"
   Goodhart 변곡점을 놓친다.

**값싼 최적화 둘** (둘 다 무손실):
- 정답 DINO CLS 를 **사전 계산해 캐시** → 정답 쪽 forward 가 0이 된다
- **프레임 서브샘플링 k=4** → DINO 성분이 프레임 평균이므로 균등 무작위 부분집합은
  전체 평균의 **불편 추정량**이다. 비용 정확히 1/4, 그래디언트 방향은 그대로

### 8.3 C 학습 **전에** 반드시 고칠 것 — 홀드아웃 누수

§6.3 에서 확정했다. 고치는 방법도 확정했다.

**하지 말 것**: 기존 홀드아웃을 바꾸는 것. 016 §9.2 와 018 의 모든 수치가 그 위에 있어서,
바꾸면 비교 가능성을 통째로 잃는다.

**할 것**: **누수 없는 홀드아웃을 하나 더 만든다.**

```python
# 분할 규칙을 재구현하지 말고 **실제 val 데이터셋을 만들어** 그 examples 를 그대로 쓴다.
# 재구현하면 셔플 순서·데이터셋 발견 순서에서 조용히 어긋난다.
ds = LeRobotSO100Dataset(root=..., train=False, val_fraction=0.05, seed=0,
                         traj_len=16, downsample=1, use_all_episodes=False)
# ds.examples 안의 에피소드만으로 홀드아웃을 만든다
```

**확인했다 — 가능하다.** (2026-08-01 18:30, `open/data/train` 실측)

```
  데이터셋 128개, 길이 ≥ 16 인 에피소드 총 11,098개
  val_fraction 0.05  →  val split 554개 / train split 10,544개

  ⇒ 96표본을 **에피소드당 1개씩** 뽑고도 458개가 남는다. 여유 있다.
    val_fraction 을 바꿀 필요도, 학습을 다시 할 필요도 없다.
```

> **덤으로 교차검증이 됐다.** train split 이 **10,544개**인데, 016 §3.2 가 적은
> "한 바퀴(epoch)를 다 돌려면 **10,544 배치**가 필요하다"와 정확히 일치한다.
> 분할 규칙에 대한 내 이해가 맞다는 독립 확인이다.

**둘 다 채점한다.** 기존 홀드아웃(비교 연속성) + 새 홀드아웃(정직한 신호).
새 홀드아웃에서만 이기고 기존에서만 이기는 상황이 나오면 그것 자체가 정보다
(전자면 일반화, 후자면 암기).

### 8.4 되돌아올 후보 (지금은 안 하지만 지우지 않는다)

| 후보 | 언제 되돌아오나 | 측정된 값 |
|---|---|---|
| **워핑 + 잔차 보정 혼합** | DINO 가 병목으로 확정되면 | DINO Δ=−0.01129, t=−2.21 (§3.8). 흐름 헤드 zero-init 이면 실패 시 순수 덧셈으로 퇴화 |
| **확산 샘플 K개 평균** | 흔들림이 크다고 나오면 | 오늘 밤 측정(§9) |
| Wan 2.2 (Apache 2.0, 5B) | 시간과 액션 배선 조건이 바뀌면 | 016 §9.4 |

---

## 9. 오늘 밤 도는 것 · 내일 할 것

### 오늘 밤 (GPU)

**확산 샘플 K개 평균** — 학습 없이, 리더보드 0.35624 를 낸 그 체크포인트로.

```
  seed 0~4 로 홀드아웃 96 을 5번 생성  (~26분 × 5 ≈ 2.2시간)
  → K 를 훑으며 평균의 값어치를 잰다
  → **먼저 흔들림을 잰다**: 샘플 간 픽셀 표준편차 / 맞혀야 할 변화량

     비율이 크면  →  분산이 많다. 평균에 여지가 있다
     비율이 작으면 →  모델이 확신을 갖고 틀린다. 평균은 소용없다 (§5.3)

  덤: 평균 영상을 mp4 로 쓴 뒤 다시 읽어 채점하므로 **인코딩 틈**도 같이 나온다 (§6.2)
```

체인: `scripts/branchC/night_ksample_20260801.sh` ·
로그: `run_logs/20260801_1806_ksample_chain.log`

**⚠ 학습 중이 아니라 5.7GB 체크포인트 쓰기가 없으므로 016 §6 의 oomd 위험은 낮다.
그래도 브라우저는 띄우지 마라.**

### 내일 (8/2, CPU)

C 설계서. §8 이 재료다. 남은 설계 결정:

- 백본 (3D conv + spatiotemporal attention / DiT / UNet)
- 잔차를 픽셀에서 낼지, 저해상도에서 내 올려붙일지 (흐림 예산 §4.3 이 제약)
- 중단 조건과 체크포인트 선택 기준
- 재현 검증용 단일 스크립트 구조 (8/10~ 에 필요)
- 누수 없는 홀드아웃 (§8.3) — **여기서 val split 표본 수를 먼저 세야 한다**

---

## 10. 방법론 기록 — 오늘 배운 것

016 §4 의 목록에 셋을 더한다.

7. **임의로 고른 파라미터 하나에 결론이 걸리면 그 파라미터를 반드시 두 점 이상 게이트에 넣는다.**
   뭉갬 배율 k=4 만 걸었는데 k=8 에서 무승부가 나왔다(§3.6). 'seed 한 쌍'과 같은 실수다.
8. **SE 를 사전 추정할 때는 가장 큰 성분의 분산부터 센다.**
   Video 분산을 빼고 계산해 3.8배 빗나갔다(§2).
9. **게이트마다 유리/불리/무승부 세 경우를 전부 정의하고 진리표에 빈칸이 없는지 확인한다.**
   Gate 2 의 '불리'를 정의하지 않아 결과를 본 뒤에 정하게 될 뻔했다.
10. **"바닥"을 만들 때 두 팔이 지는 오차가 정말 같은 종류인지 확인한다.**
    `warpnbr`(내 픽셀을 틀린 방향으로)과 `addnbr`(다른 방의 델타를 통째로 이식)을
    같은 급으로 놓았다가 결론이 뒤집혔다(§3.7②). 매개화가 다르면 비교가 아니다.
11. **새너티의 문턱은 값이 통과하는 자료형에 맞춘다.**
    float32 를 지나온 값에 1e-6 을 요구해 멀쩡한 검사가 '실패'로 찍혔다(§3.7④).
    픽셀 비교라면 **uint8 일치**로 걸어야 한다.
12. **한 축을 두 점만 재고 곡선을 안다고 하지 않는다.**
    016 은 뭉갬 배율 4배·8배만 재고 헤드룸을 −0.0169 로 적었다. 2배를 재니 −0.038 이었다.
    **끝점을 안 재면 곡선의 모양을 모른다**(§3.7⑤).

그리고 오늘 값을 한 것:

- **판정선을 결과 전에 고정한 것.** 근거 ②가 틀렸다는 것이 사후에 드러났는데,
  미리 적어 뒀기 때문에 검증되고 정정될 수 있었다(§2).
- **대리 실험의 한계를 측정 전에 적어 둔 것.** 평균내기 대리실험이 실패했을 때
  "그러므로 폐기"로 가지 않을 수 있었다(§5.3).
- **재현 검증을 먼저 한 것.** 016 §9.2 의 네 수치가 소수점 5자리까지 재현된 뒤에야
  새 숫자를 읽었다(§3.1).
