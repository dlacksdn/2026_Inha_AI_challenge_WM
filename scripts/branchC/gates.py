#!/usr/bin/env python
"""
판정선 등록 — branch C 학습의 게이트와 감시 문턱

⚠ 이 파일은 **학습을 시작하기 전에** 커밋된다. 학습 루프가 여기서 문턱을 읽는다.
   문서에만 있는 판정선은 결과를 본 뒤 조용히 바뀐다. 이 팀이 세 번 그랬다
   (016 §3.7, 018 §3.7②③). 코드가 읽는 상수는 그러기 어렵다.

수치를 바꾸려면: 왜 바꾸는지 새 문서에 적고, 그 문서 번호를 아래 CHANGES 에 남긴다.

CHANGES
  2026-08-08  최초 등록 (007 후속). 아래 유도 참조.
  2026-08-08  (b1) ρ 를 게이트에서 **진단으로 강등**. 학습 전 스모크에서 잡았다.
              이유: 내가 세운 귀무("출력이 행동에 무의존 → 순위가 무작위 순열")가
              **틀린 실패 모드를 모형화했다.** FiLM 은 행동에 대해 매끄러우므로
              어떤 조건부 신경망이든 ||out(α·a)−out(0)|| 가 α 에 단조 증가한다.
              [측정] 학습 **2스텝**짜리 모델이 ρ중앙값 = +1.00 을 냈다.
              ⇒ ρ 는 조건 붕괴를 못 잡고 **완전 단절(gradient 사망)만** 잡는다.
                G2 는 Δcos(b2) 로만 판정한다. ρ 는 계속 기록하되 문턱 판정에 안 쓴다.
              결과를 보고 기준을 바꾼 것이 아니다 — 학습을 시작하기 전이고,
              바꾼 이유는 결과가 아니라 귀무 모형의 오류다.
  2026-08-08  (a) FiLM 시간 코사인을 **DC 제거 후** 재도록 정의 수정. 문턱 0.90 은 유지.
              이유: FiLM MLP 출력의 프레임 공통(DC) 성분이 프레임별 변동분보다 훨씬 커서
              코사인이 구조적으로 1 에 붙었다. 지표가 "시간 붕괴"가 아니라 "DC 가 크다"를
              재고 있었다.
              [측정] step 2000 체크포인트: 원본 0.990 vs DC제거 0.688, DC/변동 = 9.3배.
              ⇒ 원본 정의로는 **오탐**이 떴다(step 2000 에 실제로 떴다).
              004 §4.2(a)의 의도는 "프레임 **사이**의 차이"이고 DC 는 그 차이와 무관하다.
              문턱을 바꾼 게 아니라 통계량이 주장하는 양을 재게 고친 것이다(ρ 강등과 같은 종류).
"""
from __future__ import annotations

# ══════════════════════════════════════════════════════════════════
# 감시 (계기판) — 점수가 아니다. 학습이 무너지는지만 본다
# ══════════════════════════════════════════════════════════════════

# (a) 시간 붕괴: FiLM 변조값이 16프레임 사이에서 얼마나 다른가
#     [문서] 004 §4.2(a). 프레임 간 코사인이 1 에 가까우면 16프레임이 하나로 무너지는 중
FILM_TEMPORAL_COS_MAX = 0.90

# (b1) 조건 반응: 행동을 α배 하면 출력 변화가 단조로 따라오나 (스피어만)
#      절차: holdout 고정 N 표본 × 스케일 5점(α=0.25,0.5,1,2,4) → 표본별 스피어만 → 중앙값
#      [측정] 귀무(출력이 행동에 무의존 → 순위가 무작위 순열) 분포를 40만 회 모의:
#         n=5 단일표본 스피어만의 귀무 표준편차 = 0.500  ← 단일표본은 거의 무작위다
#         중앙값의 귀무 95백분위:  N=8 → 0.400 · N=16 → 0.300 · N=32 → 0.200
#      ⇒ 004 의 "ρ<0.2" 는 유도가 없었고, N=16 에 쓰면 **귀무가 13% 확률로 통과**한다.
#        N=32 를 쓰면 0.20 이 비로소 95백분위가 된다. 비용은 검사당 160 forward(~10초).
#      ⚠ 2026-08-08 강등: 위 유도는 옳지만 **귀무 모형이 틀렸다**(CHANGES 참조).
#        학습 2스텝 모델이 ρ=+1.00 을 냈다. 매끄러운 조건부 신경망은 구조적으로 단조다.
#        ⇒ ρ 는 **완전 단절 탐지**용 진단으로만 쓴다. G2 판정에는 안 들어간다.
RHO_N_SAMPLES = 32
RHO_SCALES = (0.25, 0.5, 1.0, 2.0, 4.0)
RHO_NULL_P95 = 0.20            # N=32 중앙값의 귀무 95백분위 [측정] — 진단 참고용
RHO_DISCONNECT = 0.0           # 이 아래면 행동 경로가 아예 죽었다는 신호

# (b2) 조건 반응(직접): 진짜 행동 vs 뒤섞은 행동의 잔차 코사인 차이
#      귀무를 매 검사마다 **동시에** 잰다 — 문턱을 다른 세팅에서 이송하지 않는다(규율 6)
#      [측정] ③(action_signal_20260808_1427)에서 독립 학습한 S팔 3seed 의 산포:
#         0.1007 / 0.1153 / 0.1464 → 표준편차 ≈ 0.023 → 2σ ≈ 0.046
#      학습 seed 간 산포라 표집 잡음보다 크다. 보수적인 방향이다.
DCOS_MIN = 0.046

# (c) 잔차가 0 에서 깨어났나
#     wake = 예측 잔차 norm 이 정답 잔차 norm 의 이 비율을 **처음** 넘은 스텝
#     [문서] 005 치-2. 004 는 "0 이면 재검토"라고만 해 정상 상태를 실패로 판정했다
WAKE_RATIO = 0.05

# (d) 프레임별 잔차 크기가 뒤로 갈수록 커지나
#     [측정] 입력 이미지 = 정답 영상의 0번 프레임이므로 잔차 0번은 구조적으로 0 이다.
#       ⇒ 프로파일은 0 에서 시작해 증가해야 한다. 평평하면 첫 프레임만 다시 그리는 중.
#     통계량: 프레임 1..15 의 |잔차| 평균에 대한 선형 기울기 ÷ 전체 평균 (무차원)
PROFILE_SLOPE_MIN = 0.0        # 0 이하로 지속되면 위험

# (e) 방향: 예측 잔차 ↔ 정답 잔차 코사인
#     [문서] 001 §6-A. 005 가 "감시 5종째"로 요구했다
#     [측정] 참조점 — ③ 탐침(160×256, 소형)에서
#       L1 만 학습한 실제 데이터 0.1227 · 행동 뒤섞기 귀무 상한 0.1464 ·
#       행동으로 100% 결정되는 합성 0.6256
#     ⚠ 절대 문턱을 쓰지 않는다. (b2)의 차이값으로 판정한다 — 탐침 규모의 수치를
#       본 모델(320×512, hid_S=64)로 이송하는 것은 규율 6 위반이다

MONITOR_EVERY = 500            # 스텝. ckpt 주기와 맞춘다


# ══════════════════════════════════════════════════════════════════
# 게이트 — 통과/실패/무승부를 전부 정의한다 (018 §10-9)
# ══════════════════════════════════════════════════════════════════

GATES = {
    "G1": {
        "when": "옵티마이저 스텝 2,000",
        "check": "wake 가 발생했는가 (감시 c)",
        "pass": "wake 발생 → 계속",
        "fail": "미발생 → 학습률·초기화 재검토. 잔차 헤드 LR 부스트(001 §5-⇒2) 적용 여부 결정",
        "tie": "없음 (이분 판정)",
        "why": "005 치-2. 004 의 G1(1,000스텝)은 자기 문서의 6,000스텝(ControlNet 실측)과 "
               "모순이라 정상 상태를 실패로 판정했다. wake 정의를 넣어 해소한다",
    },
    "G2": {
        "when": "wake + 4,000 스텝",
        "check": f"감시 b2 Δcos ≥ {DCOS_MIN} (연속 2회 아니어도 됨, 단회 판정)",
        "pass": "충족 → 조건 경로 살아 있음. G2.5 로",
        "fail": "미달 → 조건 붕괴. FiLM 주입 지점 추가 또는 구조 수정",
        "tie": f"0 < Δcos < {DCOS_MIN} → 2,000 스텝 더 돌리고 재판정. "
               "두 번째도 미달이면 실패로 본다",
        "why": "005 치-1·치-2. 절대 스텝수 대신 wake 기준으로 재정의해 "
               "'깨어난 지 4,000스텝에 잴 수 있나'라는 미지수를 없앴다",
    },
    "G2.5": {
        "when": "G2 통과 후 매 감시 시점",
        "check": f"Δcos ≥ {DCOS_MIN} 가 연속 2회",
        "pass": "선명도 항(λ_f) 점화",
        "fail": "점화하지 않는다. 방향이 틀린 채 선명하게 만들면 틀린 움직임이 "
                "더 선명해질 뿐이다(018 §9-d)",
        "tie": "없음 (연속 2회는 이분 판정)",
        "why": "004 §3 의 '방향 확인 뒤 켠다'에 점화 조건이 없었다(005 치-7②)",
    },
    "G3": {
        "when": "첫 제출 (zero-init 드라이런)",
        "check": "리더보드 값이 정지영상 참조값(0.30325) ± 0.001 안인가",
        "pass": "배선 정상. 그 값을 S₀ 로 확정한다",
        "fail": "배선 버그. 원인 규명 전까지 학습 결과 제출 금지",
        "tie": "±0.001 밖 ±0.005 안 → mp4 인코딩 차이 의심. 한 번 더 만들어 재현성 확인",
        "why": "005 중-3. C 출력으로 CSV 를 만들어 본 적이 없다. 나중에 하면 "
               "배선 버그와 모델 문제가 분리 불능이 된다. "
               "zero-init 은 출력이 정확히 정지영상이라 기대값이 정해져 있다",
    },
    "G4": {
        "when": "2026-08-15",
        "check": "우리 최고 제출 vs 팀메이트 최고 Public (판정 시점 값)",
        "pass": "우리가 더 좋다 → C 계속",
        "fail": "우리가 δ=0.011 이상 나쁘다 → 남은 시간을 팀메이트 노선 지원으로 전환",
        "tie": "차이가 δ 안 → 동순위. 8/17 까지 이틀 더 태우고 재판정",
        "why": "005 치-3·중-5. 004 는 8/15 와 8/18~19 두 날짜를 갖고 있었고 "
               "동순위 밴드가 없었다. δ≈0.011 = 로컬 2·SE·√(96/65) (Public 30%≈65표본)",
    },
    "G5": {
        "when": "클린런 착수 직전",
        "check": "그때까지 최고 제출이 S₀ 보다 좋은가",
        "pass": "클린런 착수",
        "fail": "클린런 창을 튜닝 연장으로 전환. GPU 예산 40% 를 관성으로 태우지 않는다",
        "tie": "없음",
        "why": "005 중-1. 튜닝이 정지영상도 못 넘은 상태에서 클린런에 진입하는 경로가 열려 있었다",
    },
}

S0_REFERENCE = 0.30325         # 정지영상 리더보드 참조값 (8/3 초기화 이전). G3 가 갱신한다
G4_DATE = "2026-08-15"
G4_TIE_BAND = 0.011


def check_g2(rho_median: float, dcos: float) -> str:
    """G2 는 Δcos 로만 판정한다. rho 는 기록·진단용으로만 받는다(강등 사유는 CHANGES)."""
    if dcos >= DCOS_MIN:
        return "pass"
    return "tie" if dcos > 0 else "fail"


def check_g2_5(dcos_history: list[float]) -> bool:
    return len(dcos_history) >= 2 and all(d >= DCOS_MIN for d in dcos_history[-2:])


if __name__ == "__main__":
    print(__doc__)
    print(f"감시: FiLM코사인<{FILM_TEMPORAL_COS_MAX} · ρ중앙값≥{RHO_NULL_P95}(N={RHO_N_SAMPLES}) "
          f"· Δcos≥{DCOS_MIN} · wake={WAKE_RATIO} · 프로파일기울기>{PROFILE_SLOPE_MIN}")
    for k, v in GATES.items():
        print(f"\n[{k}] {v['when']}\n  검사 {v['check']}\n  통과 {v['pass']}\n"
              f"  실패 {v['fail']}\n  무승부 {v['tie']}")
